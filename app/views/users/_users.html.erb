<table>
  <th></th>
  <th><%= sort_column :username, "Логин" %></th>      
  <% unless @firm -%>    
  <th><%= sort_column :firm_id, "Фирма" %></th>    
  <% end -%>
  <th><%= sort_column :fio, "ФИО" %></th>  
  <th><%= sort_column :created_at, "Создан" %></th>  
  <th><%= sort_column :login_count, "Кол-во входов" %></th>  
  <th><%= sort_column :last_login_ip, "Пред IP" %></th>  
  <th><%= sort_column :last_login_at, "Пред. вход" %></th>  
  <th><%= sort_column :current_login_ip, "Текущ. IP" %></th>  
  <th><%= sort_column :last_request_at, "Посл. активность" %></th>
  <% @users.each do |user| -%>
    <tr>
      <td><%= checked_box_image(user.active?) %></td>
      <td><%= edit_user_link user %></td>
      <% unless @firm -%>
        <td class="no-wrap"><%= link_to user.firm.name, firm_path(user.firm) if user.firm %> <br />
        <%= stats user.firm if user.firm %>
        </td>        
      <% end -%>
      <td><%= user.fio %>
      <p class="smalltext"><%= user.appoint %></p>
      </td>
      <td class="no-wrap"><%= I18n.l(user.created_at, :format => :short) if user.created_at %></td>      
      <td><%= user.login_count %></td>
      <td><%= user.last_login_ip %></td>
      <td class="no-wrap"><%= I18n.l(user.last_login_at, :format => :short) if user.last_login_at %></td>
      <td><%= user.current_login_ip %></td>
      <td class="no-wrap"><%= I18n.l(user.last_request_at, :format => :short) if user.last_request_at %></td>
    </tr>
  <% end -%>
</table>
